[project]
name = "nutanix-migration-readiness-control-plane"
version = "0.1.0"
description = "Local-first migration readiness scoring and evidence packs for VMware-to-Nutanix planning."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
nmrcp = "nmrcp.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
